# Executable registration and codec integration plan

Status: CBOR, generic digest primitives, and the executable envelope are implemented
and aligned with the agreed Draft 0.1 contract. Runtime registration is unimplemented.
This is a greenfield project: no compatibility aliases or fallback formats exist.

Authority: `specs/compiler-spec.md`, `specs/htlk-executable.cddl`,
`specs/runtime-spec.md`, and the unified baseline in `specs/README.md`.

## Completed contract alignment

- [x] Use format `htlk.executable.graph` and exactly the `format`, `version`,
  `fingerprint`, and `payload` fields; version is `0.1` in code and CDDL/prose.
- [x] Hash exactly `UTF8("htlk.executable.graph/0.1\n") || payload`, with one LF
  and raw payload bytes, using a private incremental SHA-256 operation.
- [x] Replace golden vectors and reject alternate schemas/hash formulas. Preserve
  codec limits, immutability, tampering detection, and structured error precedence.
- [x] Update active CBOR documentation and HTLK identity examples to version 0.1.
- [x] Update the compiler/CDDL/module version table and spec checker; verify two
  independent fingerprint primitive vectors and the exact envelope field set.
- [x] Pass all 41 workspace tests, seven doc examples, formatting, Clippy, rustdoc,
  all five package builds, dependency checks, and the complete spec checker.

## Remaining documentation and CI tasks

- [ ] Replace the active link to `docs/ir-grammar.md` and its `.qir`/Lua-state
  language with current `.htlk` spec guidance; no old parser mode is required.
- [ ] Align compiler descriptions with IR compilation rather than natural-language
  interpretation. Clarify the runtime's implemented-versus-planned capabilities.
- [ ] Run `node specs/validate-specs.mjs` in CI once the spec snapshot is tracked,
  alongside the Rust suite. Its document checks are not a full graph verifier.

## Completed CBOR work

- [x] Implement validated values, deterministic encoding, strict decoding,
  per-operation accounting, and conformance tests in `htlk-cbor`.
- [x] Add compiler and runtime dependencies on `htlk-cbor`.
- [x] Verify all four workspace packages and update dependency-ordered releases.
- [x] Update licenses, notices, and permanent codec documentation.
- [x] Pass workspace formatting, Clippy, tests, rustdoc, package verification,
  and dependency-policy checks; commit and push as `f38c127`.
- [x] Remove the completed deterministic-CBOR implementation plan.
- [x] Cross-check Rust CBOR and digest output against all three source-package
  pins in `specs/examples/modules/source_bundle.json`; all match the Node helper.

## Completed digest foundation

- [x] Provide `htlk-executable::digest` with `Digest`, strict canonical text parsing,
  raw-byte SHA-256 hashing, and bounded canonical-CBOR hashing.
- [x] Wire compiler/runtime dependencies and publish executable after CBOR and before
  its consumers. No `htlk-core` facade was introduced.
- [x] Pass seven digest integration tests and three documentation examples,
  including known-answer vectors, independent CBOR preimages, and limit errors.
- [x] Pass workspace formatting, Clippy, tests, rustdoc, all five package builds,
  and dependency-policy checks; regenerate notices and include license/docs.
- [x] Migrate digest support into the public `htlk-executable::digest` module,
  preserving implementations and test vectors. Verify all 31 workspace tests,
  six documentation examples, package builds, and dependency checks; remove the
  completed migration plan.

The digest module contains general hashing primitives. Graph-record identities
and runtime acceptance policy remain pending. Each public `decode` call starts
fresh accounting.

This temporary plan carries forward unfinished consumer work from the completed
CBOR plan. Update it as registration is designed and implemented; remove it
when the tasks below are complete. It is not a claim that registration exists.

## Completed executable envelope API

The crate exposes `ExecutableEnvelope`, `EnvelopeError`, and supported format/version
constants under the contract above. Payload bytes are opaque at this outer layer;
inner CanonicalDocument validation remains registration work.

- [x] Agree and document the exact envelope fields and fingerprint preimage.
- [x] Document the contract with exact-byte vectors and deterministic diagnostic
  precedence for records with multiple schema failures.
- [x] Add `ExecutableEnvelope` with private fields and read-only accessors, plus
  `EnvelopeError` for codec, schema, format/version, and fingerprint failures.
- [x] Add construction, canonical encoding, and strict decoding using existing
  `htlk_cbor::Limits`. Do not add a `RegistrationLimits` type in this increment.
- [x] Keep format and version checks explicit. Preserve the exact payload bytes
  and verify their prescribed fingerprint before returning a checked envelope.
- [x] Test canonical bytes, round trips, schema errors, unsupported format/version,
  malformed/mismatched fingerprints, tampered payloads, codec limits, and trailing
  bytes. A valid envelope does not imply a structurally valid executable graph.
- [x] Pass ten envelope tests and remove the completed envelope plans. Remaining
  audit documentation and CI tasks have been transferred to the checklist above.

## Following increments: implement the supplied canonical payload contract

The payload schema and identity formulas are now available; do not invent another
wire format or block on a missing companion document. Follow compiler sections
4-7 and `specs/htlk-executable.cddl`, in small implementation slices:

- [ ] Add validated identifiers, canonical `ValueType`, and `Port` records.
  Distinguish identifier-keyed port maps from arbitrary string-keyed record fields.
  Implement the specified type normalization and signature-only restrictions.
- [ ] Add execution-limit and retry records, distinct from codec Limits; then
  canonical expressions, paths, functions, and prompt templates. Contract fields
  are `preconditions` and `postconditions`; omitted contracts normalize to true.
- [ ] Add scope/node/edge/operation records, including carried and next boundaries,
  deterministic ordering, local IDs, and contextual scope/loop rules.
- [ ] Add profile/engine/library/server/MCP binding records and referenced policy
  and schema documents, using actual external protocol/implementation versions.
- [ ] Assemble `CanonicalDocument` with exact fields `ir_version`, `graph_id`,
  `profile`, `root_scope`, `scopes`, `templates`, `bindings`, `libraries`,
  `schema_uris`, and `documents`.
- [ ] Add bounded record hashing with a closed kind set (scope, node, template,
  binding, server), using raw UTF-8 `htlk.<kind>/0.1\n` plus canonical record bytes.
- [ ] Implement JCS/JSON numeric rules and offline schema/document resolution;
  external JSON digests hash JCS bytes without an HTLK prefix.
- [ ] Keep raw-envelope parsing distinct from full executable validation. Reject
  empty/malformed/noncanonical or wrong-schema payloads before a graph is accepted.

## Runtime boundary and compiler consumers

- [ ] Apply finite existing codec limits to both outer and inner decoding, with
  checked allocation and documented temporary-memory overhead. The draft does
  not prescribe a public accounting session or new RegistrationLimits type.
- [ ] Decide whether deployment policy needs an additional aggregate request
  allowance. If implemented, define counters/reset/failure behavior and preserve
  per-request isolation; do not silently weaken individual document limits.
- [ ] Connect compiler production and runtime consumption to the same canonical
  document/schema/verifier implementation, with exact byte/hash fixtures.
- [ ] Implement source/SourceBundle compilation, import/export resolution,
  `inspect_module`, and graph joins per the compiler and module specs. Source
  collection and LLM planning remain host responsibilities, not compiler I/O.

## Registration validation and persistence

- [ ] Integrate the corrected `ExecutableEnvelope` checks into runtime registration,
  including format/version and exact-payload fingerprint verification, with
  contextual runtime error mapping.
- [ ] Independently decode and validate payload record schemas, references,
  content-addressed record digests, external JSON digests, and reachable closure.
- [ ] Connect the shared graph verifier and exact implementation-profile checks.
- [ ] Apply deployment attestation policy separately from structural validity.
- [ ] Map codec diagnostics into contextual runtime failures; preserve protected
  diagnostics without exposing untrusted value contents in public errors.
- [ ] Commit only fully verified immutable registrations, with same-envelope
  idempotency and same-fingerprint/different-bytes integrity failure behavior.

## Required integration checks

- [ ] Each per-document limit is tested at and beyond its boundary. If an
  aggregate request allowance is configured, also test documents that individually
  fit but jointly exceed it, plus exact aggregate boundaries and failure cleanup.
- [ ] Malformed or noncanonical inner CBOR inside a canonical envelope fails.
- [ ] Invalid fields, profile, digests, closure, or required attestations leave
  no authoritative registration behind.
- [ ] Failed decode/validation and duplicate requests obey documented accounting
  and idempotency rules; concurrent requests have independent accounting.
- [ ] Encoding the same normalized executable yields identical bytes, and the
  runtime accepts those bytes only after all structural checks pass.
- [ ] Run focused tests, workspace formatting/Clippy/tests/rustdoc/package checks,
  and update permanent documentation before deleting this plan.
