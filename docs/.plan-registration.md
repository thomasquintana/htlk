# Executable registration and codec integration plan

Status: CBOR, digest primitives, and the checked version-0.1 executable envelope
are complete, including workspace integration. Runtime registration is future
work and has not been implemented.

## Completed CBOR work

- [x] Implement validated values, deterministic encoding, strict decoding,
  per-operation accounting, and conformance tests in `htlk-cbor`.
- [x] Add compiler and runtime dependencies on `htlk-cbor`.
- [x] Verify all four workspace packages and update dependency-ordered releases.
- [x] Update licenses, notices, and permanent codec documentation.
- [x] Pass workspace formatting, Clippy, tests, rustdoc, package verification,
  and dependency-policy checks; commit and push as `f38c127`.
- [x] Remove the completed deterministic-CBOR implementation plan.

## Completed digest foundation

- [x] Provide `htlk-executable::digest` with `Digest`, strict canonical text parsing,
  raw-byte SHA-256 hashing, and bounded canonical-CBOR hashing.
- [x] Wire compiler/runtime dependencies and publish executable after CBOR and before
  its consumers. No `htlk-core` facade was introduced.
- [x] Pass seven digest integration tests and three documentation examples,
  including known-answer vectors, independent CBOR preimages, and limit errors.
- [x] Pass workspace formatting, Clippy, tests, rustdoc, all five package builds,
  and dependency-policy checks; regenerate notices and include license/docs.
- [x] Migrate digest support into the public `htlk-executable::digest` module,
  preserving implementations and test vectors. Verify all 31 workspace tests,
  six documentation examples, package builds, and dependency checks; remove the
  completed migration plan.

The digest module contains general hashing primitives. Envelope fingerprint
construction/comparison is implemented below; graph-record identities and runtime
registration acceptance policy remain among the pending tasks.

The remaining checkboxes below concern future executable production and
registration, not incomplete standalone-codec or dependency-wiring work. The
runtime library currently has no registration implementation, and each public
`decode` call still starts fresh accounting.

This temporary plan carries forward unfinished consumer work from the completed
CBOR plan. Update it as registration is designed and implemented; remove it
when the tasks below are complete. It is not a claim that registration exists.

## Completed increment: executable envelope

The executable crate exposes `ExecutableEnvelope`, `EnvelopeError`, and supported
format/version constants. Envelope version is `0.1`, format is `htlk.executable`,
and fingerprint text identifies SHA-256 of canonical CBOR
`["htlk.executable", "0.1", payload_bytes]`. All four fields are required;
payload is an exact byte string and may be empty. This contract was agreed
interactively and documented with exact-byte fixtures in the crate README.

- [x] Agree exact envelope field names/types, supported format/version values,
  fingerprint representation and domain-separated preimage, and whether an empty
  payload is permitted. Confirm against the compiler companion specification or
  define the contract explicitly together before writing wire-format code.
- [x] Document the contract with exact-byte vectors and deterministic diagnostic
  precedence for records with multiple schema failures.
- [x] Add `ExecutableEnvelope` with private fields and read-only accessors, plus
  `EnvelopeError` for codec, schema, format/version, and fingerprint failures.
- [x] Add construction, canonical encoding, and strict decoding using existing
  `htlk_cbor::Limits`. Do not add a `RegistrationLimits` type in this increment.
- [x] Keep format and version checks explicit. Preserve the exact payload bytes
  and verify their prescribed fingerprint before returning a checked envelope.
- [x] Test canonical bytes, round trips, schema errors, unsupported format/version,
  malformed/mismatched fingerprints, tampered payloads, codec limits, and trailing
  bytes. A valid envelope does not imply a structurally valid executable graph.
- [x] Pass nine envelope tests, all 40 workspace tests, seven documentation
  examples, formatting, Clippy, rustdoc, all five package builds, and dependency
  checks. Remove the completed envelope implementation plan.

Aggregate registration accounting and graph verification below remain later
registration responsibilities, separate from this envelope increment.

## Inputs and decisions needed

- [ ] Reconcile runtime/IR requirements and the agreed envelope-0.1 contract with
  the compiler companion specification: graph payload schemas, graph-record
  fingerprint preimages, and reachable-record closure still need definition.
- [ ] Define per-document limits and aggregate registration allowances. Keep
  limits as configuration and accounting as per-request mutable usage.
- [ ] Specify which counters span envelope and payload decoding: at minimum
  total visited values and copied text/byte payloads. Decide whether encoded
  input processing needs its own shared work counter.
- [ ] Keep document nesting checks explicit for each decode. Define total
  retained-memory accounting while the outer payload and decoded inner value
  coexist; do not treat payload-byte counters as exact heap measurements.
- [ ] Document deliberate charges for the outer payload byte string and its
  subsequently decoded values. A second decode must not reset the request's
  aggregate allowance, and error paths must not permit a request to regain work
  already consumed.

## Codec interface integration

- [ ] Choose the smallest interface that enforces the agreed aggregate policy:
  a bounded registration-specific composition API or an explicit accounting
  session, based on real call-site needs. Do not expose raw Encoder/Decoder state
  merely to share counters. Keep public one-shot `encode`/`decode` convenient.
- [ ] Preserve strict canonical ingress, per-document checks, checked arithmetic,
  fallible allocation, and decoder error offsets when sharing accounting.
- [ ] Define ownership of counters, reset rules, and failure behavior. Separate
  concurrent requests must never share mutable usage accidentally.
- [ ] Add compiler executable-production calls to the shared encoder when the
  executable schema exists. Test exact bytes and fingerprint preimages.
- [ ] Add runtime envelope and nested-payload decoding calls. A decoded outer
  byte string is not evidence that its contents are canonical.

## Registration validation and persistence

- [ ] Integrate the existing `ExecutableEnvelope` checks into runtime registration,
  including format/version and exact-payload fingerprint verification, with
  contextual runtime error mapping.
- [ ] Independently decode and validate payload record schemas, references,
  content-addressed record digests, external JSON digests, and reachable closure.
- [ ] Connect the shared graph verifier and exact implementation-profile checks.
- [ ] Apply deployment attestation policy separately from structural validity.
- [ ] Map codec diagnostics into contextual runtime failures; preserve protected
  diagnostics without exposing untrusted value contents in public errors.
- [ ] Commit only fully verified immutable registrations, with same-envelope
  idempotency and same-fingerprint/different-bytes integrity failure behavior.

## Required integration checks

- [ ] Each document individually fits its limits but combined usage exceeds the
  request allowance: reject before additional allocation or authoritative commit.
- [ ] Exact aggregate boundary succeeds; one unit over fails for each shared
  counter, including map keys and nested payload data.
- [ ] Malformed or noncanonical inner CBOR inside a canonical envelope fails.
- [ ] Invalid fields, profile, digests, closure, or required attestations leave
  no authoritative registration behind.
- [ ] Failed decode/validation and duplicate requests obey documented accounting
  and idempotency rules; concurrent requests have independent accounting.
- [ ] Encoding the same normalized executable yields identical bytes, and the
  runtime accepts those bytes only after all structural checks pass.
- [ ] Run focused tests, workspace formatting/Clippy/tests/rustdoc/package checks,
  and update permanent documentation before deleting this plan.
