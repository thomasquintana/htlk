# Complete executable verifier tasks 1–7

User decision: HTLK ships native Rust implementations out of the box. Use a small
Rust interpreter for the existing ExpressionKind AST and a native JSON Schema
library. No embedded language runtimes, scripting engines, guest builds, or VM
artifacts. Commit and push after each completed numbered task.

0. Completed: removed the uncommitted Wasm prototype, Lua, and the unused Tera
   dependency; aligned docs, dependency notices, and the native resource contract.
   Verified the resolved dependency graph, all 178 tests/22 doc examples, specs,
   formatting, Clippy, rustdoc, all five package builds, and dependency policy.
1. Schema admission and validation (completed): pin a native JSON Schema 2020-12
   library, disable ambient retrieval, and use the existing supplied schema catalog.
   Enforce object-root admission, meta-schema/keyword checks, supported vocabularies,
   and instance validation. Verify reference/anchor behavior against existing fixtures,
   especially URI normalization. Preserve original JCS identities.
   Apply codec/catalog limits and supported regex limits; do not claim a general
   native-validation fuel counter or hard in-process deadline. The Rust expression
   evaluator retains deterministic accounting for its own operations; operational
   watchdogs remain runtime failures, never logical false.
   Implemented native catalog compilation, ECMA-to-linear-regex checking, standard
   vocabulary/meta-schema validation, dynamic instance validation, object-root
   admission, and CanonicalDocument integration. Verified 185 tests/23 doc examples,
   formatting, Clippy, specs, rustdoc, all five package builds, and dependency policy;
   notices regenerated. The unified verified graph/profile boundary remains task 6.
2. Full MCP descriptor conformance and native URI-template expansion (completed):
   pinned published protocol schemas and license, optional metadata,
   conflicting selections, prompt result/base64 checks, normalized snapshots, and
   bounded native RFC 6570 scalar expansion are implemented. Verified all 191 tests,
   24 doc examples, specs, formatting, Clippy, rustdoc, dependency policy, and five
   package builds. Live service negotiation/authorization remains runtime work.
3. A small Rust evaluator shared by guards, pre/postconditions, until, and eval:
   lazy Boolean operations, value/absence/pending/error outcomes, strict scalar
   representations, name/type resolution, generics/callbacks, and native regex checks.
   Execution core implemented: evaluate/evaluate_condition, frozen frames and
   runtime-context interfaces, lazy state propagation, core operations, outcomes,
   rendering, callable argument metadata, regex recipes and deterministic work limits.
   Static expression analysis now resolves declared names in all branches, checks
   operators/conditions/templates, infers generic calls and fresh callback variables,
   rejects recursive/ambiguous inference, and records runtime value/projection checks.
   CheckedExpression now connects analysis to evaluated-node and boundary checks;
   runtime value admission and typed record/map/list/union projection are implemented.
   Native schema types validate against prescribed embedded roots. Schema-aware
   projection refinement and exact native-registry/callback invocation compatibility
   remain pending; task 3 is not yet complete.
   Execution-core checkpoint verified with all 200 tests and 25 doc examples,
   formatting, Clippy, specs, rustdoc, dependency policy, and five package builds.
   Static-analysis checkpoint verified with all 214 tests and 26 doc examples,
   including scoped names, generic/callback inference, deferred constraints,
   structural records, runtime obligations, and controlled-stack depth regressions.
   Formatting, Clippy, specs, rustdoc, dependency policy and package builds pass.
   Runtime-enforcement checkpoint verified with all 221 tests and 26 doc examples,
   including actual-value/schema checks, nested union absence, lazy/pending behavior,
   small projected outputs from large roots, and 512 KiB/2 MiB stack regressions.
   Formatting, Clippy, specs, rustdoc, dependency policy and five package builds pass.
4. Complete scope wait-dependency/binding/observability verification.
5. Default linked profile/manifests and policy-capability checks.
6. Unified verified executable boundary and structured validation plans/reports.
7. Complete conformance corpus, adversarial coverage, CI, and documentation.

Do not mark an adapter or partial implementation as completing a numbered task.
